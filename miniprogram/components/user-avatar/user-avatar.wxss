.avatar-wrap {
  display: inline-flex;
  flex-direction: column;
  align-items: center;
}
.avatar {
  background: #f0f0f0;
}
.name {
  font-size: 22rpx;
  color: #999;
  margin-top: 8rpx;
  max-width: 120rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

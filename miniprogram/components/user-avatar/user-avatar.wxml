<view class="avatar-wrap">
  <image class="avatar" src="{{src || '/assets/icons/default_avatar.png'}}" mode="aspectFill" style="width:{{size}}rpx;height:{{size}}rpx;border-radius:50%;" />
  <text class="name" wx:if="{{showName && name}}">{{name}}</text>
</view>
